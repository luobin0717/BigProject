const reducer = (state = {
  homeBannerList: []
},action) => {
  const { type, data } = action;
  switch (type) {
    case "":
      data:data
    default:
      return state;
  }
}
 
export default reducer;